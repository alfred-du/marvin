__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# Model weights and the llama.cpp checkout: fetched, never committed.
pi/models/
.llama.cpp/
.llama-server.log

# Eval runs are tracked on purpose: the cadence gate is only meaningful as a
# comparison across commits, and the hand-scored rules are expensive to redo.

.DS_Store
